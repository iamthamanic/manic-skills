---
page: 93
title: What is GraphQL? Is it a replacement for the REST API?
title_de: What is GraphQL? Is it a replacement for the REST API?
tags: [api, principles]
source: linkedin-posts-2024-blue.pdf
ingest: text-extract-v1
---

## Summary (EN)
- CSS divides structure (HTML) and style (CSS). This has many benefits, for example, when we
- ▶ How CSS works? CSS consists of a selector and a set of properties, which can be thought of as individual rules.
- For example, if we want to make all the text in a paragraph blue, we write CSS code like this: p { color: blue; } Here “p” is the selector and “color: blue” is the attribute that declares the color of the paragraph text
- ▶ Cascading in CSS The concept of cascading is crucial to understanding CSS. When multiple style rules conflict, the browser needs to decide which rule to use based on a specific
- ▶ Powerful Layout Capabilities of CSS In the past, CSS was only used for simple visual effects such as text colors, font styles, or
- The “Flexbox” and “Grid” layout modules are two popular CSS layout modules that make it easy to
- ▶ CSS Animation Animation and interactive elements can greatly enhance the user experience. CSS3 introduces animation features that allow us to transform and animate elements without using

## Zusammenfassung (DE)
- (Text-Extrakt — Diagramm siehe PDF Seite 93. Vision-Ingest kann DE-Summary ergänzen.)

## Diagram (what it shows)
- Refer to `assets/linkedin-posts-2024-blue.pdf` page 93 for the infographic.
- Re-run `scripts/ingest-pages.py --vision` to enrich diagram description.

## When to use / Wann nutzen
- **Use / Nutzen:** When this pattern matches your architecture question (see tags: api, principles).
- **Avoid / Vermeiden:** When simpler patterns suffice — validate with `@foundations` and `@ponytail`.

## Trade-offs
| EN | DE |
|----|-----|
| See summary bullets | Siehe Summary — bei Unsicherheit PDF Seite 93 |
